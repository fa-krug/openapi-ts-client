# Remove OpenAPI Generator Traces - Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Remove all openapi-generator traces and rebrand generated output to openapi-ts-client.

**Architecture:** Delete legacy files/templates, update header comments in Jinja2 templates, modify Python generator code, regenerate fixtures, and update README with new positioning and acknowledgements.

**Tech Stack:** Python, Jinja2 templates, pytest

---

### Task 1: Delete Root Config File

**Files:**
- Delete: `openapitools.json`

**Step 1: Delete the file**

```bash
rm openapitools.json
```

**Step 2: Verify deletion**

```bash
ls openapitools.json 2>&1 | grep -q "No such file" && echo "DELETED"
```

Expected: `DELETED`

**Step 3: Commit**

```bash
git add -u openapitools.json
git commit -m "chore: remove openapitools.json config file"
```

---

### Task 2: Delete Angular Templates (openapi-generator artifacts)

**Files:**
- Delete: `src/openapi_ts_client/templates/angular/.gitignore.j2`
- Delete: `src/openapi_ts_client/templates/angular/.openapi-generator-ignore.j2`
- Delete: `src/openapi_ts_client/templates/angular/git_push.sh.j2`
- Delete: `src/openapi_ts_client/templates/angular/VERSION.j2`

**Step 1: Delete template files**

```bash
rm src/openapi_ts_client/templates/angular/.gitignore.j2
rm src/openapi_ts_client/templates/angular/.openapi-generator-ignore.j2
rm src/openapi_ts_client/templates/angular/git_push.sh.j2
rm src/openapi_ts_client/templates/angular/VERSION.j2
```

**Step 2: Verify deletion**

```bash
ls src/openapi_ts_client/templates/angular/.gitignore.j2 2>&1 | grep -q "No such file" && echo "DELETED"
```

Expected: `DELETED`

**Step 3: Commit**

```bash
git add -u src/openapi_ts_client/templates/angular/
git commit -m "chore: remove openapi-generator artifact templates"
```

---

### Task 3: Update infrastructure.py

**Files:**
- Modify: `src/openapi_ts_client/generators/angular/infrastructure.py`

**Step 1: Remove artifact templates from STATIC_FILES list**

Change lines 22-33 from:
```python
# Static infrastructure files (no template variables needed)
STATIC_FILES = [
    "index.ts.j2",
    "api.module.ts.j2",
    "provide-api.ts.j2",
    "variables.ts.j2",
    "encoder.ts.j2",
    "param.ts.j2",
    "query.params.ts.j2",
    ".gitignore.j2",
    ".openapi-generator-ignore.j2",
    "git_push.sh.j2",
]
```

To:
```python
# Static infrastructure files (no template variables needed)
STATIC_FILES = [
    "index.ts.j2",
    "api.module.ts.j2",
    "provide-api.ts.j2",
    "variables.ts.j2",
    "encoder.ts.j2",
    "param.ts.j2",
    "query.params.ts.j2",
]
```

**Step 2: Remove .openapi-generator directory creation**

Delete lines 104-136 (the entire block that creates `.openapi-generator` directory, VERSION file, and FILES manifest):

```python
    # Create .openapi-generator directory
    openapi_gen_dir = output_path / ".openapi-generator"
    openapi_gen_dir.mkdir(exist_ok=True)

    # Generate VERSION file
    version_template = env.get_template("VERSION.j2")
    version_content = version_template.render()
    (openapi_gen_dir / "VERSION").write_text(version_content)

    # Build FILES manifest - all files sorted alphabetically
    all_files = []

    # Add root-level files
    for f in generated_files:
        all_files.append(f)

    # Add api/ files
    all_files.append("api/api.ts")
    if service_files:
        for sf in service_files:
            all_files.append(f"api/{sf}")

    # Add model/ files
    if model_files:
        for mf in model_files:
            all_files.append(f"model/{mf}")

    # Sort all files alphabetically
    all_files.sort()

    # Write FILES manifest
    files_content = "\n".join(all_files) + "\n"
    (openapi_gen_dir / "FILES").write_text(files_content)
```

**Step 3: Run tests to verify no syntax errors**

```bash
python -c "from openapi_ts_client.generators.angular.infrastructure import generate_infrastructure; print('OK')"
```

Expected: `OK`

**Step 4: Commit**

```bash
git add src/openapi_ts_client/generators/angular/infrastructure.py
git commit -m "refactor: remove openapi-generator directory creation from Angular generator"
```

---

### Task 4: Update Axios Templates (branding)

**Files:**
- Modify: `src/openapi_ts_client/templates/axios/index.ts.j2`
- Modify: `src/openapi_ts_client/templates/axios/api.ts.j2`
- Modify: `src/openapi_ts_client/templates/axios/base.ts.j2`
- Modify: `src/openapi_ts_client/templates/axios/common.ts.j2`
- Modify: `src/openapi_ts_client/templates/axios/configuration.ts.j2`

**Step 1: Update each template**

In each file, replace:
```
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
```

With:
```
 * NOTE: This file is auto generated by openapi-ts-client.
 * https://github.com/fa-krug/openapi-ts-client
```

**Step 2: Verify changes**

```bash
grep -l "openapi-ts-client" src/openapi_ts_client/templates/axios/*.j2 | wc -l
```

Expected: `5`

**Step 3: Commit**

```bash
git add src/openapi_ts_client/templates/axios/
git commit -m "chore: rebrand Axios templates to openapi-ts-client"
```

---

### Task 5: Update Fetch Templates (branding)

**Files:**
- Modify: `src/openapi_ts_client/templates/fetch/runtime.ts.j2`
- Modify: `src/openapi_ts_client/templates/fetch/model.ts.j2`
- Modify: `src/openapi_ts_client/templates/fetch/api.ts.j2`

**Step 1: Update each template**

In each file, replace:
```
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
```

With:
```
 * NOTE: This file is auto generated by openapi-ts-client.
 * https://github.com/fa-krug/openapi-ts-client
```

**Step 2: Verify changes**

```bash
grep -l "openapi-ts-client" src/openapi_ts_client/templates/fetch/*.j2 | wc -l
```

Expected: `3`

**Step 3: Commit**

```bash
git add src/openapi_ts_client/templates/fetch/
git commit -m "chore: rebrand Fetch templates to openapi-ts-client"
```

---

### Task 6: Update Angular Templates (branding)

**Files:**
- Modify: `src/openapi_ts_client/templates/angular/api.base.service.ts.j2`
- Modify: `src/openapi_ts_client/templates/angular/model.ts.j2`
- Modify: `src/openapi_ts_client/templates/angular/service.ts.j2`
- Modify: `src/openapi_ts_client/templates/angular/extracted_type.ts.j2`

**Step 1: Update each template**

In each file, replace:
```
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
```

With:
```
 * NOTE: This file is auto generated by openapi-ts-client.
 * https://github.com/fa-krug/openapi-ts-client
```

**Step 2: Verify changes**

```bash
grep -l "openapi-ts-client" src/openapi_ts_client/templates/angular/*.j2 | wc -l
```

Expected: `4`

**Step 3: Commit**

```bash
git add src/openapi_ts_client/templates/angular/
git commit -m "chore: rebrand Angular templates to openapi-ts-client"
```

---

### Task 7: Delete Fixture Artifacts (petstore)

**Files:**
- Delete: `tests/fixtures/petstore/angular/.openapi-generator/` (directory)
- Delete: `tests/fixtures/petstore/angular/.openapi-generator-ignore`
- Delete: `tests/fixtures/petstore/angular/.gitignore`
- Delete: `tests/fixtures/petstore/angular/git_push.sh`
- Delete: `tests/fixtures/petstore/axios/.openapi-generator/` (directory)
- Delete: `tests/fixtures/petstore/axios/.openapi-generator-ignore`
- Delete: `tests/fixtures/petstore/axios/.gitignore`
- Delete: `tests/fixtures/petstore/axios/git_push.sh`
- Delete: `tests/fixtures/petstore/fetch/.openapi-generator/` (directory)
- Delete: `tests/fixtures/petstore/fetch/.openapi-generator-ignore`

**Step 1: Delete petstore fixture artifacts**

```bash
rm -rf tests/fixtures/petstore/angular/.openapi-generator
rm -f tests/fixtures/petstore/angular/.openapi-generator-ignore
rm -f tests/fixtures/petstore/angular/.gitignore
rm -f tests/fixtures/petstore/angular/git_push.sh

rm -rf tests/fixtures/petstore/axios/.openapi-generator
rm -f tests/fixtures/petstore/axios/.openapi-generator-ignore
rm -f tests/fixtures/petstore/axios/.gitignore
rm -f tests/fixtures/petstore/axios/git_push.sh

rm -rf tests/fixtures/petstore/fetch/.openapi-generator
rm -f tests/fixtures/petstore/fetch/.openapi-generator-ignore
```

**Step 2: Verify deletion**

```bash
ls tests/fixtures/petstore/*/.openapi-generator 2>&1 | grep -q "No such file" && echo "DELETED"
```

Expected: `DELETED`

**Step 3: Commit**

```bash
git add -u tests/fixtures/petstore/
git commit -m "chore: remove openapi-generator artifacts from petstore fixtures"
```

---

### Task 8: Delete Fixture Artifacts (space_zoo)

**Files:**
- Delete: `tests/fixtures/space_zoo/angular/.openapi-generator/` (directory)
- Delete: `tests/fixtures/space_zoo/angular/.openapi-generator-ignore`
- Delete: `tests/fixtures/space_zoo/angular/.gitignore`
- Delete: `tests/fixtures/space_zoo/angular/git_push.sh`
- Delete: `tests/fixtures/space_zoo/axios/.openapi-generator/` (directory)
- Delete: `tests/fixtures/space_zoo/axios/.openapi-generator-ignore`
- Delete: `tests/fixtures/space_zoo/axios/.gitignore`
- Delete: `tests/fixtures/space_zoo/axios/git_push.sh`
- Delete: `tests/fixtures/space_zoo/fetch/.openapi-generator/` (directory)
- Delete: `tests/fixtures/space_zoo/fetch/.openapi-generator-ignore`

**Step 1: Delete space_zoo fixture artifacts**

```bash
rm -rf tests/fixtures/space_zoo/angular/.openapi-generator
rm -f tests/fixtures/space_zoo/angular/.openapi-generator-ignore
rm -f tests/fixtures/space_zoo/angular/.gitignore
rm -f tests/fixtures/space_zoo/angular/git_push.sh

rm -rf tests/fixtures/space_zoo/axios/.openapi-generator
rm -f tests/fixtures/space_zoo/axios/.openapi-generator-ignore
rm -f tests/fixtures/space_zoo/axios/.gitignore
rm -f tests/fixtures/space_zoo/axios/git_push.sh

rm -rf tests/fixtures/space_zoo/fetch/.openapi-generator
rm -f tests/fixtures/space_zoo/fetch/.openapi-generator-ignore
```

**Step 2: Verify deletion**

```bash
ls tests/fixtures/space_zoo/*/.openapi-generator 2>&1 | grep -q "No such file" && echo "DELETED"
```

Expected: `DELETED`

**Step 3: Commit**

```bash
git add -u tests/fixtures/space_zoo/
git commit -m "chore: remove openapi-generator artifacts from space_zoo fixtures"
```

---

### Task 9: Regenerate Petstore Fixtures

**Files:**
- Regenerate: `tests/fixtures/petstore/fetch/` (all TypeScript files)
- Regenerate: `tests/fixtures/petstore/axios/` (all TypeScript files)
- Regenerate: `tests/fixtures/petstore/angular/` (all TypeScript files)

**Step 1: Regenerate petstore fixtures**

```python
import json
from pathlib import Path
from openapi_ts_client import generate_typescript_client, ClientFormat

spec_path = Path("tests/fixtures/petstore/openapi.json")
spec = json.loads(spec_path.read_text())

# Regenerate all three formats
generate_typescript_client(spec, ClientFormat.FETCH, "tests/fixtures/petstore/fetch")
generate_typescript_client(spec, ClientFormat.AXIOS, "tests/fixtures/petstore/axios")
generate_typescript_client(spec, ClientFormat.ANGULAR, "tests/fixtures/petstore/angular")
```

Run:
```bash
python -c "
import json
from pathlib import Path
from openapi_ts_client import generate_typescript_client, ClientFormat

spec_path = Path('tests/fixtures/petstore/openapi.json')
spec = json.loads(spec_path.read_text())

generate_typescript_client(spec, ClientFormat.FETCH, 'tests/fixtures/petstore/fetch')
generate_typescript_client(spec, ClientFormat.AXIOS, 'tests/fixtures/petstore/axios')
generate_typescript_client(spec, ClientFormat.ANGULAR, 'tests/fixtures/petstore/angular')
print('Petstore fixtures regenerated')
"
```

Expected: `Petstore fixtures regenerated`

**Step 2: Verify new branding in generated files**

```bash
grep -l "openapi-ts-client" tests/fixtures/petstore/fetch/runtime.ts && echo "OK"
```

Expected: `tests/fixtures/petstore/fetch/runtime.ts` followed by `OK`

**Step 3: Commit**

```bash
git add tests/fixtures/petstore/
git commit -m "chore: regenerate petstore fixtures with openapi-ts-client branding"
```

---

### Task 10: Regenerate Space Zoo Fixtures

**Files:**
- Regenerate: `tests/fixtures/space_zoo/fetch/` (all TypeScript files)
- Regenerate: `tests/fixtures/space_zoo/axios/` (all TypeScript files)
- Regenerate: `tests/fixtures/space_zoo/angular/` (all TypeScript files)

**Step 1: Regenerate space_zoo fixtures**

```bash
python -c "
import json
from pathlib import Path
from openapi_ts_client import generate_typescript_client, ClientFormat

spec_path = Path('tests/fixtures/space_zoo/openapi.json')
spec = json.loads(spec_path.read_text())

generate_typescript_client(spec, ClientFormat.FETCH, 'tests/fixtures/space_zoo/fetch')
generate_typescript_client(spec, ClientFormat.AXIOS, 'tests/fixtures/space_zoo/axios')
generate_typescript_client(spec, ClientFormat.ANGULAR, 'tests/fixtures/space_zoo/angular')
print('Space Zoo fixtures regenerated')
"
```

Expected: `Space Zoo fixtures regenerated`

**Step 2: Verify new branding**

```bash
grep -l "openapi-ts-client" tests/fixtures/space_zoo/fetch/runtime.ts && echo "OK"
```

Expected: `tests/fixtures/space_zoo/fetch/runtime.ts` followed by `OK`

**Step 3: Commit**

```bash
git add tests/fixtures/space_zoo/
git commit -m "chore: regenerate space_zoo fixtures with openapi-ts-client branding"
```

---

### Task 11: Update README.md

**Files:**
- Modify: `README.md`

**Step 1: Add "Why This Project?" section after Features**

Insert after line 16 (after the Features section):

```markdown

## Why This Project?

Python backend frameworks like FastAPI and django-ninja already generate OpenAPI specs from your type hints. This library enables those frameworks to take the next step: generating TypeScript clients that mirror your backend types exactly. Framework authors can integrate openapi-ts-client to give their users end-to-end type safety with zero manual type definitions.
```

**Step 2: Add "Acknowledgements" section before License**

Insert before the License section (before line ~447):

```markdown

## Acknowledgements

This project builds on the excellent work of the [OpenAPI Generator](https://openapi-generator.tech/) team. Their years of effort creating robust code generation templates provided the foundation that made openapi-ts-client possible. Thank you for your contributions to the open-source community.
```

**Step 3: Verify sections exist**

```bash
grep -q "Why This Project" README.md && grep -q "Acknowledgements" README.md && echo "OK"
```

Expected: `OK`

**Step 4: Commit**

```bash
git add README.md
git commit -m "docs: add project positioning and acknowledgements to README"
```

---

### Task 12: Run Tests

**Step 1: Run full test suite**

```bash
pytest -v
```

Expected: All tests pass

**Step 2: Verify no openapi-generator references remain in source**

```bash
grep -r "openapi-generator" src/ --include="*.py" --include="*.j2" | grep -v "# " | wc -l
```

Expected: `0`

**Step 3: Final commit if any remaining changes**

```bash
git status --porcelain | grep -q . && git add -A && git commit -m "chore: cleanup remaining changes" || echo "Nothing to commit"
```

---

### Task 13: Final Verification

**Step 1: Count openapi-generator references in fixtures**

```bash
grep -r "OpenAPI Generator" tests/fixtures/ --include="*.ts" | wc -l
```

Expected: `0`

**Step 2: Count openapi-ts-client references in fixtures**

```bash
grep -r "openapi-ts-client" tests/fixtures/ --include="*.ts" | head -5
```

Expected: Multiple files with new branding

**Step 3: Verify no .openapi-generator directories exist**

```bash
find tests/fixtures -name ".openapi-generator" -type d | wc -l
```

Expected: `0`
